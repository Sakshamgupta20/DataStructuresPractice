package Graphs;

public class StateColor {
    int node;
    int color;
    int steps;
    StateColor(int node, int color, int steps) {
        this.node = node;
        this.color = color;
        this.steps = steps;
    }
}
